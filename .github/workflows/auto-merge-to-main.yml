name: Auto-merge to main

# Pushing to claude/* or fix/* auto-merges that branch into main (then Vercel deploys).
# Concurrency: one merge at a time so two pushes don't race (second would fail on push).
on:
  push:
    branches:
      - 'claude/**'
      - 'fix/**'

concurrency:
  group: auto-merge-to-main
  cancel-in-progress: false

jobs:
  merge:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Merge to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git checkout main
          git merge --ff-only origin/${{ github.ref_name }} || \
            git merge -m "chore: auto-merge from ${{ github.ref_name }}" origin/${{ github.ref_name }}
          git push origin main
