# Training-only dependencies — used by train_lgbm.py and GitHub Actions retrain cron.
# NOT needed on Railway. Install separately: pip install -r requirements-train.txt
nba-api>=1.10
pandas>=2.0
numpy>=1.24
lightgbm==4.3.0
scikit-learn>=1.0
